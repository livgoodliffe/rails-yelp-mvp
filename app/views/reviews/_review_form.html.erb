<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection: Review::STARS %>

  <%= f.submit "Add review", class: "btn text-white", style:"background-color: #6ea4f7;" %>
  <% end %>
